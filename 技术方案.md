
```md
# 体型模拟器前端技术方案

- **版本**：V1.0
- **阶段**：纯前端 MVP
- **目标**：用户通过调整身高、体重，实时查看 3D 人体模型的体型变化。
- **参考产品**：Body Visualizer 类网站
- **后端依赖**：无
- **AI 依赖**：无（后续可扩展）

---

## 1. 项目目标

实现一个网页端 3D 体型模拟器，用户可通过滑块调整：

- 身高
- 体重
- 模型类型（女性 / 男性）
- 视角（正面、侧面、背面、自由旋转）

页面中的 3D 人体模型根据参数实时变化：

```text
身高变化：
- 腿部长度变化
- 躯干长度变化
- 手臂长度变化
- 保持头部和脚部相对自然

体重变化：
- 全身胖瘦变化
- 腹部变化
- 腰部变化
- 臀部变化
- 大腿变化
- 手臂变化
- 胸部区域变化
```

---

## 2. 功能范围

### 2.1 MVP 功能

| 功能 | 说明 |
|---|---|
| 模型切换 | 女性模型、男性模型 |
| 身高滑块 | 调整人体高度和四肢比例 |
| 体重滑块 | 调整体脂、腹部、腰臀腿和手臂轮廓 |
| BMI 展示 | 根据身高体重计算 BMI |
| 视角切换 | 正面、左侧、右侧、背面 |
| 自由旋转 | 鼠标拖动或移动端手势旋转 |
| 缩放查看 | 滚轮缩放或双指缩放 |
| 重置参数 | 恢复默认身高、体重和视角 |
| 本地保存 | 使用 LocalStorage 保存用户参数 |
| 截图下载 | 将当前 3D 人体导出为 PNG 图片 |

### 2.2 后续扩展功能

```text
- 胸围、腰围、臀围调节
- 肩宽、大腿围、小腿围调节
- 体脂率、肌肉量调节
- 梨形、苹果型、沙漏型等身材预设
- 当前体型与目标体型对比
- AI 真人风格效果图
- 虚拟试衣
- 用户账号和云端保存
- 尺码推荐
```

---

## 3. 非目标与说明

本项目属于体型可视化工具，不属于医学测量工具。

页面需展示免责声明：

> 模拟结果基于参数化 3D 人体模型生成，仅用于体型可视化参考，不代表真实人体测量、医学诊断、体脂检测或健康建议。

不应使用以下宣传语：

```text
- 医学级准确
- 精确复刻真人体型
- 精确预测体脂
- 准确诊断身材问题
```

---

## 4. 总体技术架构

```text
┌───────────────────────────────────────────────┐
│                    浏览器                       │
│                                               │
│  React UI                                     │
│  ├── 身高、体重滑块                            │
│  ├── 模型类型选择                              │
│  ├── 正侧背视角控制                            │
│  ├── BMI 展示                                 │
│  └── 截图下载                                 │
│                                               │
│  Zustand 状态管理                             │
│  └── 身高、体重、视角、模型类型等状态            │
│                                               │
│  体型参数计算模块                              │
│  ├── 身高 → 骨骼缩放参数                        │
│  └── 体重 → Morph Target 权重                  │
│                                               │
│  React Three Fiber + Three.js                 │
│  ├── 加载 GLB 人体模型                         │
│  ├── 控制骨骼                                 │
│  ├── 控制 Shape Key / BlendShape               │
│  ├── 渲染材质、灯光、相机和阴影                 │
│  └── WebGL 实时显示                            │
│                                               │
│  LocalStorage                                 │
│  └── 保存本地用户参数                           │
└───────────────────────────────────────────────┘
```

数据流：

```text
用户拖动身高 / 体重滑块
        ↓
更新 Zustand 状态
        ↓
计算身高骨骼缩放和体重 Morph 参数
        ↓
更新 GLB 模型骨骼与 Shape Key 权重
        ↓
Three.js / WebGL 实时渲染
```

---

## 5. 技术栈

### 5.1 前端技术栈

| 技术 | 用途 | License |
|---|---|---|
| React | 页面 UI 框架 | MIT |
| TypeScript | 类型约束 | 开源免费 |
| Vite | 开发与构建工具 | MIT |
| Three.js | WebGL 3D 渲染 | MIT |
| React Three Fiber | React 方式操作 Three.js | MIT |
| `@react-three/drei` | Three.js 常用工具组件 | MIT |
| Zustand | 状态管理与本地持久化 | MIT |
| Tailwind CSS | 页面样式，可选 | MIT |
| Blender | 3D 建模、骨骼、Shape Key 制作 | GPL |
| GLB / glTF | 网页端 3D 模型格式 | 开放标准 |

### 5.2 推荐依赖

```bash
npm install three @react-three/fiber @react-three/drei zustand
npm install -D typescript vite @types/three
```

可选 UI 工具：

```bash
npm install lucide-react clsx
```

---

## 6. 核心前提：人体模型必须支持变形

前端不能自动把一个静态模型变成自然的胖瘦人体。

如果模型只是普通静态网格，例如：

```text
.obj
.fbx
.glb
```

但没有骨骼和 Shape Key，前端只能做：

```text
整体变大
整体变小
整体拉高
整体拉宽
```

这种效果不自然。

因此，人体模型必须提前具备：

```text
- 骨骼绑定（Armature / Rig）
- Skinned Mesh（蒙皮）
- Shape Key / BlendShape（形态键）
- 身高修正 Shape Key
- PBR 材质
- 基础服装或服装贴图
```

核心原则：

```text
Blender / 3D 模型负责：定义人体可以如何变形
前端 Three.js 负责：根据滑块参数控制变形
```

---

## 7. 人体模型资产规范

### 7.1 模型文件

建议分别制作两个模型：

```text
female-adult-v1.glb
male-adult-v1.glb
```

不建议将一个模型强行变形成不同性别模型。

### 7.2 基础模型参数

女性模型示例：

```text
基础身高：165cm
基础体重：55kg
基础姿势：A-Pose
脚底位置：Y = 0
模型单位：米
```

男性模型示例：

```text
基础身高：175cm
基础体重：70kg
基础姿势：A-Pose
脚底位置：Y = 0
模型单位：米
```

### 7.3 模型命名规范

人体网格名称：

```text
Body
Clothes
Hair
```

骨骼名称建议：

```text
Hips
Spine
Spine1
Chest
Neck
Head

LeftUpperArm
LeftForeArm
LeftHand

RightUpperArm
RightForeArm
RightHand

LeftThigh
LeftCalf
LeftFoot

RightThigh
RightCalf
RightFoot
```

---

## 8. Shape Key / BlendShape 设计

### 8.1 MVP 必需 Shape Key

第一版只支持身高和体重时，建议至少包含：

```text
body_fat            全身胖瘦变化
belly_large         腹部突出
chest_large         胸部区域变化
waist_large         腰腹变化
hip_large           臀部、骨盆变化
thigh_large         大腿变化
calf_large          小腿变化
arm_large           手臂变化

height_torso_fix    躯干拉伸修正
height_leg_fix      腿部拉伸修正
height_arm_fix      手臂拉伸修正
```

### 8.2 Shape Key 制作原则

| 参数 | 主要影响区域 |
|---|---|
| `body_fat` | 全身脂肪感、整体体积 |
| `belly_large` | 腹部前突、腰腹厚度 |
| `waist_large` | 腰侧、下腹、后腰 |
| `hip_large` | 臀部、骨盆、臀腿连接 |
| `thigh_large` | 大腿前后内外侧 |
| `calf_large` | 小腿围度 |
| `arm_large` | 上臂、前臂 |
| `chest_large` | 胸廓、胸部轮廓 |

不应通过简单横向缩放实现体重变化，否则人体会像气球。

---

## 9. 身高变形方案

### 9.1 不推荐方式

```ts
model.scale.set(1, 1.1, 1);
```

问题：

```text
- 头部会被拉长
- 脚部会被拉长
- 手掌比例失真
- 躯干和腿部比例不自然
```

### 9.2 推荐方式：骨骼分段缩放

身高变化应由不同身体部位共同承担。

| 身体部位 | 身高变化承担比例 |
|---|---:|
| 躯干 | 25% |
| 大腿 | 40% |
| 小腿 | 25% |
| 手臂 | 10% |
| 头部 | 基本不变 |
| 脚部 | 少量变化或不变 |

示例：

```ts
const BASE_HEIGHT_CM = 165;

export function calculateHeightScale(heightCm: number) {
  const ratio = heightCm / BASE_HEIGHT_CM;
  const delta = ratio - 1;

  return {
    torso: 1 + delta * 0.25,
    thigh: 1 + delta * 0.4,
    calf: 1 + delta * 0.25,
    upperArm: 1 + delta * 0.05,
    forearm: 1 + delta * 0.05,
  };
}
```

应用骨骼缩放：

```ts
import * as THREE from "three";

export function applyHeight(
  bones: Record<string, THREE.Bone>,
  heightCm: number,
) {
  const scale = calculateHeightScale(heightCm);

  bones.Spine.scale.y = scale.torso;
  bones.Spine1.scale.y = scale.torso;

  bones.LeftThigh.scale.y = scale.thigh;
  bones.RightThigh.scale.y = scale.thigh;

  bones.LeftCalf.scale.y = scale.calf;
  bones.RightCalf.scale.y = scale.calf;

  bones.LeftUpperArm.scale.y = scale.upperArm;
  bones.RightUpperArm.scale.y = scale.upperArm;

  bones.LeftForeArm.scale.y = scale.forearm;
  bones.RightForeArm.scale.y = scale.forearm;
}
```

### 9.3 身高修正 Shape Key

骨骼拉伸后，可能造成膝盖、臀部、腋下、衣物连接不自然。

需配合：

```text
height_torso_fix
height_leg_fix
height_arm_fix
```

示例：

```ts
const heightFix = Math.min(
  1,
  Math.abs(heightCm - BASE_HEIGHT_CM) / 25,
);
```

---

## 10. 体重变形方案

### 10.1 BMI 计算

```ts
export function calculateBMI(heightCm: number, weightKg: number) {
  const heightM = heightCm / 100;
  return weightKg / (heightM * heightM);
}
```

BMI 仅作为全身体型趋势参考。

### 10.2 体重映射规则

```text
身高 + 体重
        ↓
BMI
        ↓
全身体脂趋势
        ↓
body_fat、belly、waist、hip、thigh、arm 等 Morph Target
```

示例代码：

```ts
function clamp(value: number, min = 0, max = 1) {
  return Math.max(min, Math.min(max, value));
}

export function getWeightMorphs(
  heightCm: number,
  weightKg: number,
) {
  const bmi = calculateBMI(heightCm, weightKg);

  return {
    bodyFat: clamp((bmi - 17) / 18),
    belly: clamp((bmi - 21) / 13),
    chest: clamp((bmi - 18) / 18),
    waist: clamp((bmi - 20) / 14),
    hip: clamp((bmi - 19) / 15),
    thigh: clamp((bmi - 19) / 15),
    calf: clamp((bmi - 21) / 18),
    arm: clamp((bmi - 19) / 15),
  };
}
```

### 10.3 设置 Morph Target

```ts
import * as THREE from "three";

export function setMorph(
  mesh: THREE.SkinnedMesh,
  name: string,
  value: number,
) {
  const index = mesh.morphTargetDictionary?.[name];

  if (
    index === undefined ||
    !mesh.morphTargetInfluences
  ) {
    return;
  }

  mesh.morphTargetInfluences[index] = value;
}
```

应用体重变化：

```ts
export function applyWeight(
  mesh: THREE.SkinnedMesh,
  heightCm: number,
  weightKg: number,
) {
  const morphs = getWeightMorphs(heightCm, weightKg);

  setMorph(mesh, "body_fat", morphs.bodyFat);
  setMorph(mesh, "belly_large", morphs.belly);
  setMorph(mesh, "chest_large", morphs.chest);
  setMorph(mesh, "waist_large", morphs.waist);
  setMorph(mesh, "hip_large", morphs.hip);
  setMorph(mesh, "thigh_large", morphs.thigh);
  setMorph(mesh, "calf_large", morphs.calf);
  setMorph(mesh, "arm_large", morphs.arm);
}
```

---

## 11. 平滑动画方案

用户拖动滑块时，模型不应突然跳变。

使用 Three.js 的平滑插值：

```ts
import * as THREE from "three";

export function smoothValue(
  current: number,
  target: number,
  delta: number,
) {
  return THREE.MathUtils.damp(current, target, 10, delta);
}
```

目标体验：

```text
- 滑块拖动后立即响应
- 人体在 100ms ~ 250ms 内自然过渡
- 不出现明显跳帧
- 不频繁触发页面重渲染
```

---

## 12. 前端目录结构

```text
body-visualizer/
├── public/
│   ├── models/
│   │   ├── female-adult-v1.glb
│   │   └── male-adult-v1.glb
│   ├── hdr/
│   │   └── studio-small.hdr
│   └── textures/
│
├── src/
│   ├── app/
│   │   └── App.tsx
│   │
│   ├── components/
│   │   ├── BodyCanvas.tsx
│   │   ├── BodyModel.tsx
│   │   ├── BodyControls.tsx
│   │   ├── ViewControls.tsx
│   │   ├── StatsPanel.tsx
│   │   └── ScreenshotButton.tsx
│   │
│   ├── stores/
│   │   └── bodyStore.ts
│   │
│   ├── lib/
│   │   ├── bodyMath.ts
│   │   ├── bodyHeight.ts
│   │   ├── bodyMorph.ts
│   │   ├── modelUtils.ts
│   │   └── storage.ts
│   │
│   ├── types/
│   │   └── body.ts
│   │
│   ├── styles/
│   │   └── globals.css
│   │
│   └── main.tsx
│
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 13. 状态管理设计

### 13.1 类型定义

```ts
export type ModelType = "female" | "male";

export type ViewMode =
  | "front"
  | "left"
  | "right"
  | "back"
  | "free";

export interface BodyProfile {
  modelType: ModelType;
  heightCm: number;
  weightKg: number;
  viewMode: ViewMode;
}
```

### 13.2 Zustand Store

```ts
import { create } from "zustand";
import { persist } from "zustand/middleware";

interface BodyStore {
  modelType: "female" | "male";
  heightCm: number;
  weightKg: number;
  viewMode: "front" | "left" | "right" | "back" | "free";

  setModelType: (modelType: "female" | "male") => void;
  setHeightCm: (heightCm: number) => void;
  setWeightKg: (weightKg: number) => void;
  setViewMode: (
    viewMode: "front" | "left" | "right" | "back" | "free",
  ) => void;

  reset: () => void;
}

export const useBodyStore = create<BodyStore>()(
  persist(
    (set) => ({
      modelType: "female",
      heightCm: 165,
      weightKg: 55,
      viewMode: "front",

      setModelType: (modelType) => set({ modelType }),
      setHeightCm: (heightCm) => set({ heightCm }),
      setWeightKg: (weightKg) => set({ weightKg }),
      setViewMode: (viewMode) => set({ viewMode }),

      reset: () =>
        set({
          modelType: "female",
          heightCm: 165,
          weightKg: 55,
          viewMode: "front",
        }),
    }),
    {
      name: "body-visualizer-profile",
    },
  ),
);
```

---

## 14. React Three Fiber 场景

### 14.1 Canvas 配置

```tsx
import { Canvas } from "@react-three/fiber";
import {
  ContactShadows,
  Environment,
  OrbitControls,
} from "@react-three/drei";

export function BodyCanvas() {
  return (
    <Canvas
      shadows
      dpr={[1, 2]}
      camera={{
        position: [0, 1.35, 3.2],
        fov: 35,
      }}
      gl={{
        antialias: true,
        preserveDrawingBuffer: true,
      }}
    >
      <ambientLight intensity={0.5} />

      <directionalLight
        position={[3, 5, 4]}
        intensity={2}
        castShadow
      />

      <Environment files="/hdr/studio-small.hdr" />

      <BodyModel />

      <ContactShadows
        position={[0, 0.01, 0]}
        opacity={0.35}
        blur={2}
        scale={8}
      />

      <OrbitControls
        enablePan={false}
        minDistance={2}
        maxDistance={5}
      />
    </Canvas>
  );
}
```

### 14.2 人体模型组件

```tsx
import { useGLTF } from "@react-three/drei";
import { useEffect, useMemo } from "react";
import * as THREE from "three";

import { useBodyStore } from "../stores/bodyStore";
import { applyHeight } from "../lib/bodyHeight";
import { applyWeight } from "../lib/bodyMorph";

export function BodyModel() {
  const modelType = useBodyStore((state) => state.modelType);
  const heightCm = useBodyStore((state) => state.heightCm);
  const weightKg = useBodyStore((state) => state.weightKg);

  const modelPath =
    modelType === "female"
      ? "/models/female-adult-v1.glb"
      : "/models/male-adult-v1.glb";

  const { scene } = useGLTF(modelPath);

  const { bodyMesh, bones } = useMemo(() => {
    let bodyMesh: THREE.SkinnedMesh | null = null;
    const bones: Record<string, THREE.Bone> = {};

    scene.traverse((object) => {
      if (
        object instanceof THREE.SkinnedMesh &&
        object.name === "Body"
      ) {
        bodyMesh = object;
      }

      if (object instanceof THREE.Bone) {
        bones[object.name] = object;
      }
    });

    return { bodyMesh, bones };
  }, [scene]);

  useEffect(() => {
    if (!bodyMesh) return;

    applyHeight(bones, heightCm);
    applyWeight(bodyMesh, heightCm, weightKg);
  }, [bodyMesh, bones, heightCm, weightKg]);

  return <primitive object={scene} />;
}
```

> 注意：实际项目中，多个模型实例不能直接共享同一个 `scene`。需要使用 `SkeletonUtils.clone(scene)` 或 `Clone` 组件复制模型。

---

## 15. 控制面板示例

```tsx
import { useBodyStore } from "../stores/bodyStore";
import { calculateBMI } from "../lib/bodyMath";

export function BodyControls() {
  const heightCm = useBodyStore((state) => state.heightCm);
  const weightKg = useBodyStore((state) => state.weightKg);

  const setHeightCm = useBodyStore((state) => state.setHeightCm);
  const setWeightKg = useBodyStore((state) => state.setWeightKg);

  const bmi = calculateBMI(heightCm, weightKg);

  return (
    <aside className="body-controls">
      <h2>体型参数</h2>

      <label>
        <span>身高：{heightCm} cm</span>
        <input
          type="range"
          min="140"
          max="200"
          step="1"
          value={heightCm}
          onChange={(event) =>
            setHeightCm(Number(event.target.value))
          }
        />
      </label>

      <label>
        <span>体重：{weightKg} kg</span>
        <input
          type="range"
          min="35"
          max="130"
          step="1"
          value={weightKg}
          onChange={(event) =>
            setWeightKg(Number(event.target.value))
          }
        />
      </label>

      <div className="bmi-card">
        <span>BMI</span>
        <strong>{bmi.toFixed(1)}</strong>
      </div>
    </aside>
  );
}
```

---

## 16. 视角控制

相机预设：

```ts
export const CAMERA_VIEWS = {
  front: {
    position: [0, 1.35, 3.2],
    target: [0, 1.0, 0],
  },
  back: {
    position: [0, 1.35, -3.2],
    target: [0, 1.0, 0],
  },
  left: {
    position: [-3.2, 1.35, 0],
    target: [0, 1.0, 0],
  },
  right: {
    position: [3.2, 1.35, 0],
    target: [0, 1.0, 0],
  },
};
```

建议使用：

```text
@react-three/drei 的 CameraControls
```

实现平滑转场，而不是相机瞬移。

---

## 17. 材质与视觉效果

第一版不使用 AI，也可以通过材质和灯光提升真实感。

### 17.1 建议使用 PBR 贴图

```text
BaseColor / Albedo：基础肤色
Normal Map：皮肤细节
Roughness Map：皮肤粗糙度
AO Map：环境遮蔽
```

Three.js 示例：

```ts
const material = new THREE.MeshPhysicalMaterial({
  map: skinColorMap,
  normalMap: skinNormalMap,
  roughnessMap: skinRoughnessMap,
  roughness: 0.5,
  metalness: 0,
  clearcoat: 0.02,
});
```

### 17.2 光照建议

```text
- HDRI 环境光
- 主方向光
- 接触阴影
- 浅灰或暖灰背景
- 中性摄影棚效果
```

不建议使用纯黑背景和单色蓝色材质，否则模型会像扫描假人。

### 17.3 服装建议

MVP 建议默认展示：

```text
运动内衣 + 运动短裤
或紧身运动上衣 + 紧身裤
```

第一版优先选择：

```text
将基础运动服作为人体贴图或材质的一部分。
```

优点：

```text
- 不会穿模
- 模型变胖变瘦时服装自然跟随
- 开发成本低
```

---

## 18. 性能要求

| 项目 | 目标 |
|---|---:|
| 单个 GLB 模型大小 | 8MB ～ 20MB |
| 模型三角面数量 | 2 万 ～ 5 万 |
| 贴图大小 | 1K ～ 2K |
| 滑块响应时间 | 小于 100ms |
| 桌面端帧率 | 50 ～ 60 FPS |
| 中端手机帧率 | 25 ～ 40 FPS |
| 首屏模型加载 | 3 ～ 5 秒内 |

优化手段：

```text
- GLB / glTF 格式
- Meshopt 或 Draco 网格压缩
- KTX2 / Basis 贴图压缩
- 移动端降低 DPR
- 移动端降低阴影质量
- HDR 文件使用轻量版本
- 模型和贴图懒加载
- 提供加载进度条
```

推荐优先使用：

```text
glTF Transform
Meshopt Compression
KTX2 Texture Compression
```

---

## 19. 截图导出

前端可通过 Canvas 导出 PNG。

```ts
export function downloadCanvasImage(
  canvas: HTMLCanvasElement,
) {
  const imageUrl = canvas.toDataURL("image/png");

  const link = document.createElement("a");
  link.href = imageUrl;
  link.download = "body-visualizer.png";
  link.click();
}
```

注意事项：

```text
- Canvas 需设置 preserveDrawingBuffer: true
- 贴图和 HDR 资源必须允许 CORS
- 导出前可隐藏 UI 面板
- 可增加透明背景和白底背景选项
```

---

## 20. Blender 模型制作流程

```text
1. 制作基础人体网格
2. 统一单位、比例和坐标方向
3. 制作人体骨骼
4. 完成蒙皮和权重绘制
5. 制作局部 Shape Key
6. 制作身高修正 Shape Key
7. 制作基础服装
8. 制作皮肤与服装 PBR 材质
9. 导出 GLB
10. 在网页中测试骨骼和 Morph Target
11. 根据测试结果反复调整模型
```

3D 美术交付文件建议包含：

```text
female-adult-v1.blend
female-adult-v1.glb

male-adult-v1.blend
male-adult-v1.glb

skin_basecolor.png
skin_normal.png
skin_roughness.png
skin_ao.png

body-model-spec.md
asset-license.md
```

`body-model-spec.md` 应记录：

```text
- 模型名称
- 基础身高与体重
- 骨骼名称清单
- Shape Key 名称清单
- 网格名称
- 材质名称
- 模型单位
- GLB 导出版本
```

---

## 21. 开发阶段

### 阶段 0：模型检查

检查当前模型是否包含：

```text
□ 原始 Blender 文件
□ 骨骼 Armature
□ Skinned Mesh
□ Shape Key
□ 可导出 GLB
□ 模型商用授权
□ 贴图和服装授权
```

如果当前模型只是静态蓝色人体模型，需要先进行 Blender 改造。

---

### 阶段 1：静态 3D 展示

功能：

```text
- 加载 GLB
- 旋转与缩放
- 正面、侧面、背面
- 基础光照、阴影和背景
- 响应式页面布局
```

预计工作量：

```text
2 ～ 4 天
```

---

### 阶段 2：身高调节

功能：

```text
- 身高滑块
- 骨骼分段缩放
- 身高修正 Shape Key
- 相机自动适配人体高度
```

预计工作量：

```text
3 ～ 7 天
```

---

### 阶段 3：体重调节

功能：

```text
- 体重滑块
- BMI 计算
- 全身体脂 Shape Key
- 腹部、腰、臀、腿、手臂局部 Shape Key
- 平滑动画
```

预计工作量：

```text
3 ～ 7 天
```

---

### 阶段 4：体验完善

功能：

```text
- 本地参数保存
- 模型切换
- 默认体型预设
- 重置功能
- 截图下载
- 加载状态
- 移动端适配
- 性能优化
```

预计工作量：

```text
3 ～ 5 天
```

---

## 22. 验收标准

### 功能验收

| 功能 | 验收条件 |
|---|---|
| 身高调节 | 高度明显变化，头部和脚部不过度拉伸 |
| 体重调节 | 腹部、腰、臀、手臂、大腿变化自然 |
| 模型旋转 | 鼠标和触屏手势可正常操作 |
| 视角切换 | 可正确显示正面、侧面和背面 |
| 重置 | 可恢复默认身高、体重、视角 |
| 本地保存 | 刷新页面后参数仍可恢复 |
| 截图 | 可下载当前模型 PNG 图片 |

### 视觉验收

```text
- 模型没有破面
- 四肢和躯干连接自然
- 胖瘦变化不呈气球感
- 极端参数下不出现严重穿模
- 运动服不明显穿模
- 灯光、材质和阴影自然
```

### 性能验收

```text
- 桌面端 Chrome 中拖动滑块基本流畅
- 中端移动设备可正常操作
- 首屏显示加载状态
- 资源加载时间不超过 5 秒
- 滑块调整后的模型更新小于 100ms
```

---

## 23. 风险与处理方案

| 风险 | 原因 | 处理方案 |
|---|---|---|
| 模型无法自然变形 | 没有骨骼或 Shape Key | 在 Blender 中重新绑定和制作形态键 |
| 身高变化不自然 | 使用整体缩放 | 使用骨骼分段缩放 + 修正 Shape Key |
| 体重变化像气球 | 全身统一缩放 | 使用腹部、腰、臀、腿、手臂局部 Morph |
| 衣物穿模 | 衣物未同步变形 | MVP 使用贴图服装，后续制作衣物 Morph |
| 手机端卡顿 | 面数、贴图和阴影过高 | 压缩模型、降低 DPR、关闭高质量阴影 |
| 模型授权风险 | 资源来源不明 | 使用自制资产、购买商用授权或签署外包协议 |
| 模拟不够精确 | 身高体重不能唯一决定体型 | 明确仅为可视化参考，后续增加三围与体脂率 |

---

## 24. MVP 最终技术决策

```text
前端框架：
React + TypeScript + Vite

3D 渲染：
Three.js + React Three Fiber + drei

状态管理：
Zustand + LocalStorage

3D 制作：
Blender

模型格式：
GLB / glTF

核心变形：
身高 = 骨骼分段缩放 + 身高修正 Shape Key
体重 = body_fat + 腹部 / 腰 / 臀 / 大腿 / 手臂局部 Shape Key

视觉效果：
PBR 材质 + HDRI 环境光 + 阴影 + 基础运动服

后端：
第一阶段不需要

AI：
第一阶段不需要，后续作为异步高质量成图能力扩展
```

---

## 25. 下一步行动项

在开始前端开发前，优先确认人体模型是否满足以下条件：

```text
□ 是否有 `.blend` 原始文件
□ 是否有骨骼绑定
□ 是否是 Skinned Mesh
□ 是否有 Shape Key / Morph Target
□ 是否可导出为 `.glb`
□ 是否具备可商用授权
□ 是否可修改网格、贴图和服装
```

如果当前模型是静态模型，下一步应优先完成：

> 在 Blender 中将模型改造成具备骨骼和 Shape Key 的可参数化人体模型。

完成后，前端即可使用 Three.js 控制身高、体重和后续三围变化。
```